if __name__ == '__main__':
    N = int(input())
    result_list = []
    
    for _ in range(N):
        # Read the line and split into command and arguments
        parts = input().split()
        command = parts[0]
        
        # Mapping command strings to list methods
        if command == "insert":
            result_list.insert(int(parts[1]), int(parts[2]))
        elif command == "print":
            print(result_list)
        elif command == "remove":
            result_list.remove(int(parts[1]))
        elif command == "append":
            result_list.append(int(parts[1]))
        elif command == "sort":
            result_list.sort()
        elif command == "pop":
            result_list.pop()
        elif command == "reverse":
            result_list.reverse()
